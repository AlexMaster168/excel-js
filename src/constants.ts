import type {CellStyles} from '@/redux/types'

export const defaultStyles: CellStyles = {
  textAlign: 'left',
  fontWeight: 'normal',
  textDecoration: 'none',
  fontStyle: 'normal'
}

export const defaultTitle = 'Новая таблица'
export const defaultSheetName = 'Лист1'

export const defaultRows = 20
export const defaultCols = 26
